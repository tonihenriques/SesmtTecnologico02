@using GISModel.Entidades

@{
    ViewBag.Title = "Consulta Empregado";
}

<div class="row">
    <div class="col-xs-12">
        <h3 class="header smaller lighter blue">
            Empregados
            @*<a href="@Url.Action("Novo", "Empregado")" class="btn btn-white btn-info btn-bold" style="float: right; margin-top: -2px;">
                <i class="ace-icon fa fa-plus bigger-120 blue"></i>
                Novo Empregado
            </a>*@
        </h3>
    </div>
</div>

<div class="row">
    <div class="col-xs-12">

        @{
            Html.RenderPartial("_MensagensDeRetorno");
        }



        @if (ViewBag.ListaEmpregado != null)
        {
            <div>
                <table id="dynamic-table" class="table table-striped table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>Nome</th>
                            <th>CPF</th>
                            <th class="hidden-480">Email</th>
                            <th width="89px"></th>
                        </tr>
                    </thead>

                    <tbody>

                        @foreach (var iEmpregado in ViewBag.ListaEmpregado)
                        {

                            @*<tr id="linha-@iEmpregado.AdmissaoID">*@
                            <tr>

                                <td>@iEmpregado.Nome</td>
                                <td>@iEmpregado.CPF</td>
                                <td>@iEmpregado.Email</td>

                                <td>
                                    <div class="hidden-sm hidden-xs action-buttons">
                                        <a class="red" href="#" onclick="BuscarDetalhesEmpregado('@iEmpregado.IDEmpregado'); return false; ">
                                            <i class="ace-icon fa fa-search bigger-130" title="Detalhes do Empregado"></i>
                                        </a>
                                        
                                    </div>

                                    <div class="hidden-md hidden-lg">
                                        <div class="inline pos-rel">
                                            <button class="btn btn-minier btn-yellow dropdown-toggle" data-toggle="dropdown" data-position="auto">
                                                <i class="ace-icon fa fa-caret-down icon-only bigger-120"></i>
                                            </button>

                                            <ul class="dropdown-menu dropdown-only-icon dropdown-yellow dropdown-menu-right dropdown-caret dropdown-close">

                                                <li>
                                                    <a href="#" class="tooltip-info" onclick="BuscarDetalhesEmpregado('@iEmpregado.IDEmpregado'); return false;" data-rel="tooltip" title="View">
                                                        <span class="blue">
                                                            <i class="fa fa-industry" aria-hidden="true"></i>
                                                        </span>
                                                    </a>
                                                </li>
                                            </ul>
                                        </div>
                                    </div>
                                </td>
                            </tr>





                        }

                    </tbody>
                </table>
            </div>

        }

    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jQ_dataTable")
    <script src="~/Scripts/Custom/Empregado/Listagem.js"></script>
}
